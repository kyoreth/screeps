var roleMiner =  {
  run: function(creep) {
    var sources = creep.room.find(FIND_SOURCES);
    if(targets.length > 0){
      if(creep.transfer(targets[0], RESOURCE_ENERGY) == ERR_NOT_IN_RANGE){
        creeo.moveTo(targets[0],  {visualizePathStyle: {stroke: '#ffffff'}});
      }
    }

  }


}
